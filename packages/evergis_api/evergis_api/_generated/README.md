# Spatial Processing Core API

**Version:** 1.5.1.0

## Installation

Recommended way is to install the package from this directory:

```bash
# in directory with generated code
pip install -e .  # or without -e: pip install .
```

Dependencies from pyproject.toml will be installed automatically:

- httpx>=0.24.0
- pydantic>=2.0.0
- typing-extensions>=4.0.0

Alternative without installation (temporary run):

```bash
PYTHONPATH=$(pwd) python your_script.py
```

## Usage

After installation, import necessary objects from modules of this package.
For example, modules `client.py` (synchronous clients) and `async_client.py` (asynchronous clients)
and `schemas/` package with schemas.
Specific class and function names depend on the generated code.

## Structure

- `client.py` — HTTP clients for working with API (sync)
- `async_client.py` — HTTP clients for working with API (async)
- `schemas/` — Pydantic data models
- `__init__.py` — root package module
- `pyproject.toml` — package metadata and dependencies

## Auto-generation

This code is automatically generated by EverGIS Python API Generator.
